create database dbsetting;
use dbsetting;
create table customer(
                         CID int (3)primary key,
                         CName varchar(10),
                         CAddres varchar(25),
                         CMobile varchar(10)
);